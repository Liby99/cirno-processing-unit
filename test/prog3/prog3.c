const int MEM_SIZE = 256;

int main() {
  char * mem = calloc(sizeof(char) * MEM_SIZE);
  setup(mem);
  prog2(mem);
  test(mem);
}

void setup(char* mem) {
  // Setup
}

void prog3(char* mem) {
  // What i need to write
}

void test(char* mem) {
  // What i need to test
}